// import { render } from '@testing-library/react'
// import '@testing-library/jest-dom'
// import { Header } from './Header'

// test('renders the header component', () => {
//   const { getByText, getByAltText } = render(<Header />)

//   // Assert that the logo is rendered
//   const logo = getByAltText('DT Money Logo')
//   expect(logo).toBeInTheDocument()

//   // Assert that the title is rendered
//   const title = getByText('DT Money')
//   expect(title).toBeInTheDocument()
// })

// test('renders the modal create transaction component', () => {
//   const { getByTestId } = render(<Header />)

//   // Assert that the modal create transaction component is rendered
//   const modalCreateTransaction = getByTestId('modal-create-transaction')
//   expect(modalCreateTransaction).toBeInTheDocument()
// })
